namespace zvs
{    
    public enum DataType
    {
        NONE = 0,
        INTEGER,
        DECIMAL,
        STRING,
        BYTE,
        BOOL,
        SHORT,
        LIST,
        COMPORT
    }
}
