<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             x:Class="zvs.WPF.ScheduledTaskControls.IntervalTaskUserControl"
             x:Name="This"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel Orientation="Vertical">
        <GroupBox Header="Start Time" >
        <xctk:DateTimePicker Value="{Binding IntervalScheduledTask.StartTime, ElementName=This, UpdateSourceTrigger=PropertyChanged}" Width="300" HorizontalAlignment="Left" Margin="10"/>
            </GroupBox>
        <GroupBox  Header="Interval" Margin="0 10" >
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="Repeat every"/>
                <TextBox  Margin="10,0,10,0" Width="76" Text="{Binding IntervalScheduledTask.RepeatIntervalInSeconds, ElementName=This, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="seconds" />
            </StackPanel>
        </GroupBox>
    </StackPanel>
</UserControl>
