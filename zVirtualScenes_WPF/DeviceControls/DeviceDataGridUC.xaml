<UserControl x:Class="zVirtualScenes_WPF.DeviceControls.DeviceDataGridUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
              xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" xmlns:my="clr-namespace:zVirtualScenesCommon.Entity;assembly=zVirtualScenesCommon" 
             Loaded="UserControl_Loaded" PreviewKeyDown="UserControl_PreviewKeyDown" 
             PreviewMouseRightButtonDown="UserControl_PreviewMouseRightButtonDown" >
    <UserControl.Resources>
        <Style x:Key="Body_Content_DataGrid_Centering"
        TargetType="{x:Type DataGridCell}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Grid Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <CollectionViewSource x:Key="devicesViewSource" d:DesignSource="{d:DesignInstance my:device, CreateList=True}">           
        </CollectionViewSource>        
    </UserControl.Resources>
    
    <Grid DataContext="{StaticResource devicesViewSource}">
        <DataGrid x:Name="DeviceGrid" CanUserAddRows="False"  CellStyle="{StaticResource Body_Content_DataGrid_Centering}" HorizontalGridLinesBrush="#FFE2E2E2" 
                  AlternatingRowBackground="#FFFFFCFC" GridLinesVisibility="Horizontal" RowHeight="29" AutoGenerateColumns="False" 
                  ItemsSource="{Binding}" PreviewMouseLeftButtonDown="DeviceGrid_PreviewMouseLeftButtonDown" RowEditEnding="DeviceGrid_RowEditEnding" RowHeaderWidth="0" AllowDrop="True">
            <DataGrid.Columns>

                <DataGridTemplateColumn MaxWidth="9" MinWidth="9" Width="9"  Header="" CanUserResize="False" CanUserReorder="False">
                    <DataGridTemplateColumn.CellTemplate >
                        <DataTemplate>
                            <Image Name="DragImage" Cursor="Hand" Width="9" Height="28" Source="/zVirtualScenes_WPF;component/Images/drag_icon.png" Margin="0,0,0,0" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn MaxWidth="30" MinWidth="30" Width="30"  Header="" CanUserResize="False" CanUserReorder="False">
                    <DataGridTemplateColumn.CellTemplate >
                        <DataTemplate>
                            <Image Name="img" Width="30" Height="20" Source="/zVirtualScenes_WPF;component/Images/20radio.png" />
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding DeviceTypeName}" Value="THERMOSTAT">
                                    <Setter TargetName="img" Property="Source" Value="pack://application:,,,/zVirtualScenes_WPF;component/Images/20thermostat.png"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding DeviceTypeName}" Value="DIMMER">
                                    <Setter TargetName="img" Property="Source" Value="pack://application:,,,/zVirtualScenes_WPF;component/Images/20bulb.png"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding DeviceTypeName}" Value="SWITCH">
                                    <Setter TargetName="img" Property="Source" Value="pack://application:,,,/zVirtualScenes_WPF;component/Images/20switch.png"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding DeviceTypeName}" Value="CONTROLLER">
                                    <Setter TargetName="img" Property="Source" Value="pack://application:,,,/zVirtualScenes_WPF;component/Images/20controller.png"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding DeviceTypeName}" Value="DOORLOCK">
                                    <Setter TargetName="img" Property="Source" Value="pack://application:,,,/zVirtualScenes_WPF;component/Images/20doorlock.png"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn x:Name="NodeID"  MinWidth="50"  Width="25" Header="ID" Binding="{Binding node_id}" IsReadOnly="True" 
                                    CanUserResize="False" CanUserReorder="False"  FontSize="14" />

                <DataGridTextColumn MinWidth="200" Width="*" Header="Name" Binding="{Binding friendly_name, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="False"   />

                <DataGridTemplateColumn x:Name="group2" MinWidth="100" Header="GROUPS" CanUserResize="True">
                    <DataGridTemplateColumn.CellTemplate >
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding Path=group_devices}" SelectedIndex="0" Margin="3,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Path=group.name}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ComboBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn x:Name="devicetype" Width="150" MinWidth="50" Header="Type" Binding="{Binding DeviceTypeFriendlyName}" IsReadOnly="True" />

                <DataGridTemplateColumn x:Name="LevelBar" Header="" CanUserResize="False" Width="66" MinWidth="66" MaxWidth="66">
                    <DataGridTemplateColumn.CellTemplate >
                        <DataTemplate>
                            <ProgressBar Name="LevelBar" Value="{Binding LevelMeter, Mode=OneWay}"   Width="60" Height="25" Margin="3,0"/>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding DeviceTypeName}" Value="THERMOSTAT">
                                    <Setter TargetName="LevelBar" Property="Foreground" Value="#FF0088DE"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn x:Name="LevelTxt" Header="Level" Width="40" MinWidth="40" MaxWidth="40" CanUserResize="False" Binding="{Binding LevelText}" IsReadOnly="True"/>

                <DataGridTextColumn x:Name="Date" Header="Last Repoll" Width="120" MinWidth="120" MaxWidth="120" Binding="{Binding last_heard_from}" IsReadOnly="True" FontSize="10" />

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

